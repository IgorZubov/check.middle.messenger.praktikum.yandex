export const BASE_QUERY = '#app';

export enum ERouter {
    LOGIN = '/',
    REGISTRATION = '/sign-up',
    MESSENGER = '/messenger',
    SETTINGS = '/settings',
    PROFILE_EDIT = '/profile-edit',
    SERVER_ERROR = '/server-error',
    NOT_FOUND = '*',
}
