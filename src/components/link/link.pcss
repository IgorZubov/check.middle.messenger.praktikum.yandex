.link {
    font-weight: 500;
    font-size: 12px;
    line-height: 12px;
    color: var(--blue);
    margin: 0;
    padding: 0;
    text-decoration: none;

    &:hover {
        color: var(--blue-hover);
        text-decoration: underline;
    }
}
